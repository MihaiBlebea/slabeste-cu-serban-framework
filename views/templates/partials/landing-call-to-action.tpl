<div class="{$bg_color}">
    <div id="{$id}" class="pt-5 pb-5 d-flex justify-content-center">
        <div class="col-md-6 justify-content-center">
            <h3 class="text-center mb-3">{$title}</h3>
            <button data-toggle="modal" data-target="#modalRegister" type="button" class="col btn {$color} btn-lg">{$cta}</button>
        </div>
    </div>
</div>
